# STUDY-V2-008 — Rollback

No application source was changed during this verification/documentation pass, so there is no code rollback to apply.

If the checkpoint documentation itself must be removed, remove only `study v2/checkpoints/STUDY-V2-008/` additions and `study v2/reports/REPORT_STUDY-V2-008.md`; do not restore or roll back any STUDY-V2-003 through STUDY-V2-007 files.

The `before/` and `after/` snapshots are record copies, not rollback inputs.
