Verbatim pre-edit snippets for `index.html`, captured from this session's own
Edit tool calls.

## 1. `<link>` — before

```html
  <link rel="stylesheet" href="style-admin.css" />
  <link rel="stylesheet" href="assets/vendor/leaflet/leaflet.css" />
```

(`<link rel="stylesheet" href="style-study.css" />` was inserted between these two lines.)

## 2. `<script>` block A — before

```html
  <script src="services/map-note-service.js"></script>
  <script src="app-admin.js"></script>
```

(Three new script tags — `data/study-subjects.js`, `data/study-resource-manifest.js`,
`services/study-resource-service.js` — were inserted between these two lines.)

## 3. `<script>` block B — before

```html
  <script src="app-router.js"></script>
  <script src="app-community.js"></script>
  <script src="app-wall.js"></script>
```

(`<script src="app-study.js"></script>` was inserted between the `app-community.js`
and `app-wall.js` lines.)
